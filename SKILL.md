---
name: price-action-analysis
description: 基于 Al Brooks/阿布价格行为学把用户提供的 K 线图制作成带通道、区间、箭头、关键价位、形态标签和交易建议的标注分析图，并输出下一步交易计划。Use when the user provides a candlestick chart screenshot or OHLC chart data and asks to mark up the chart, create a price-action analysis image, read price action, identify market cycle/Always In/breakouts/channels/ranges/reversals, or suggest next trade scenarios from chart structure; excludes pure fundamental analysis, news-only forecasts, automated strategy coding, and investment recommendations without chart context.
---

# 价格行为分析

## 核心原则

这个 skill 的主产物是一张标注分析图，而不是单纯文字解读。用户给出 K 线图时，必须尽量在原图上叠加价格行为标注，并附上简洁交易计划。

把用户提供的图表、OHLC 数据或明确行情片段作为唯一事实源。不要凭空补图、补价格、补成交细节；如果用户问当前行情、最新价格或具体标的的实时判断，先获取最新数据或要求用户提供图表，再进入分析。

使用阿布价格行为学作为主要分析语言：先判断背景和市场周期，再看具体形态。图上文字默认使用中文术语；除非用户要求保留英文，英文术语只放在正文补充，不放进主标注。

输出目标是“一张图同时完成历史复盘和下一步交易计划”，不是确定性预测。需要给交易建议时，必须同时给出失效条件、风险点、不交易条件和替代场景。

禁止把历史复盘写成事后诸葛亮。允许分析和标注前面走势，但每个历史复盘标签都必须还原“当时能看到的证据 -> 当时可执行的动作/不动作 -> 当时的失效点 -> 后续如何验证或推翻”。最后一段行情必须单独给出下一步交易计划。

## 工作流

1. 判断请求类型：
   - 标注分析图：用户给出 K 线截图，希望在同一张图里看到历史复盘和接下来交易计划。
   - 交易计划图：围绕当前最后一段走势，标出下一步多空触发、失效和目标区域；如果图上有明显历史结构，也保留必要复盘证据。
   - 交易复盘图：在原图上标出当时背景、形态质量、进出场问题和更优处理。
   - 概念解释：解释价格行为术语、阿布体系、某个形态或市场周期；没有图时只输出文字和示意说明。
   - 学习训练：把一段行情拆成读图练习、问题清单或复盘模板。

2. 检查输入是否足够：
   - 标注分析图必须有图表截图或可渲染的 OHLC 图表数据。
   - 交易建议至少需要最后可见价格区域、周期、关键结构或用户提供的交易意图。
   - 复盘至少需要进场、出场、止损或预期，以及当时图表背景。
   - 信息不足但可继续时，输出“条件性标注”；信息不足会导致编造时，先只问最少的关键问题。

3. 按固定顺序读图：
   - 分层：先把图分成“历史复盘区”和“当前计划区”。历史复盘区解释已经发生的关键交易机会；当前计划区回答下一步怎么做。
   - 当前决策点：确定最后一根可见 K 线、当前价格所在区域、离最近失效点有多远。
   - 市场周期：趋势、通道、交易区间、突破阶段、反转尝试，或状态不明。
   - 持续方向：如果必须选边，当前更偏多、偏空，还是区间中性；说明改变判断需要什么证据。
   - 关键位置：前高前低、区间边界、趋势线/通道线、突破点、缺口、整数位、测量目标和明显磁吸位。
   - 多空力量：连续强 K、实体大小、尾巴、重叠程度、回调深度、突破后的后续跟随。
   - 形态质量：信号 K、入场位置、是否二次进场、是否在支撑阻力附近、是否处在趋势尾端或区间中央。
   - 交易数学：估计概率、风险、潜在回报和失效点；避免只说“看涨/看跌”。
   - 历史复盘链：每个关键历史点都压成 `当时证据 -> 当时计划 -> 当时失效 -> 后续结果`。
   - 当前决策树：把下一步结论压成 `如果出现什么证据 -> 才做什么 -> 止损/失效在哪里 -> 目标在哪里 -> 什么情况不做`。

4. 设计标注图：
   - 在同一张图上同时放两类标注：历史复盘标注和当前计划标注。
   - 历史复盘标注只选关键转折和关键交易点，不从左到右贴满所有波段。每个标签必须包含当时逻辑，不只写事后名称。
   - 当前计划标注必须放在最后可见价格附近，标出当前位置、最近交易区间边界、突破/失败突破触发位、止损/失效位、目标区、当前不交易理由。
   - 视觉权重建议：历史复盘 50%-60%，当前计划 40%-50%。如果用户强调“接下来怎么做”，当前计划权重上调。
   - 使用半透明色块表示交易区间、窄幅震荡、通道或供需区域；使用箭头表示方向切换、突破失败、买卖信号；使用水平线表示关键价位和测量目标。
   - 历史复盘标签优先写“当时证据 + 当时动作/失效”，例如 `强跌后反弹弱：只等空头二次进场`、`区间上沿失败：多头离场`。
   - 当前计划标签优先写“当前条件 + 交易动作”，例如 `区间中部：不追价`、`上破并跟随：等回测多`、`跌破失败：空头撤退`。
   - 图上文字必须放在可读底板上，不能超出画布，不能被半透明色块、K 线或价格轴挡住；右侧和底部标签要预留边距。
   - 不把大段解释塞进图里；详细逻辑放在图后面的交易建议里。

5. 生成标注产物：
   - 先根据读图结果写出 annotation JSON，格式见 `references/annotation-spec.md`。
   - 使用 `scripts/render_annotations.py` 把原图和 annotation JSON 渲染为标注图；输出后缀为 `.png` 时生成位图，输出后缀为 `.svg` 时生成自包含 SVG。
   - 默认优先交付 PNG，必要时同时交付 SVG 和 annotation JSON，方便继续编辑。

6. 输出结构按任务选择：
   - 标注分析图：`分析图路径 -> 历史复盘要点 -> 当前决策点 -> 证据链 -> 下一步交易计划 -> 失效条件 -> 不交易条件`。
   - 交易计划图：`分析图路径 -> 当前是否可交易 -> 多头触发 -> 空头触发 -> 失效/止损 -> 目标 -> 不交易条件`。
   - 交易复盘图：`分析图路径 -> 历史复盘要点 -> 当前计划缺口 -> 可复用规则`。
   - 概念解释：`定义 -> 在图上的识别方式 -> 有效背景 -> 失败信号 -> 常见误用`。

7. 交付前自检：
   - 已经产出可打开的标注图文件，或明确说明为什么当前不能生成图。
   - 没有用孤立形态替代背景判断。
   - 没有把低确定性的场景写成结论。
   - 没有遗漏失败场景、止损逻辑或不交易条件。
   - 历史复盘没有停留在事后命名；每个历史点都说明当时证据、当时计划、失效点和后续验证。
   - 当前计划没有缺席；图和正文都明确最后可见价格附近下一步怎么做。
   - 图上文字不遮挡最关键的 K 线、价格轴或当前最后一段走势；文字不超出图片边界，且没有被色块压住。
   - 如果使用了实时事实，已经说明数据来源和时间。

## 资源

需要调用阿布价格行为核心模型、术语、读图顺序和常见误用时，读取 `references/price-action-framework.md`。

制作标注图前，读取 `references/annotation-spec.md`，按其中 JSON 结构规划标注元素。

生成分析图时，执行 `scripts/render_annotations.py <chart-image> <annotations.json> <output.png|output.svg>`。该脚本只负责确定性渲染，不替代价格行为判断。

最终输出前，尤其是图表分析、交易计划和复盘任务，读取 `references/quality-gate.md` 做质量检查。
